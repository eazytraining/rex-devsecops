apiVersion: v1
kind: Service
metadata:
  name: recipe-app
  labels:
    app: recipe
    tier: svc-frontend
spec:
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080
  selector:
    tier: frontend
  type: NodePort
