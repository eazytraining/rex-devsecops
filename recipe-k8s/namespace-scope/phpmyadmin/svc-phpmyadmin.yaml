apiVersion: v1
kind: Service
metadata:
  name: phpmyadmin
  namespace: recipe-ns
  labels:
    app: phpmyadmin
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 80
  selector:
    app: phpmyadmin
