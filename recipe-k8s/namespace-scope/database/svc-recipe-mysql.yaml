apiVersion: v1
kind: Service
metadata:
  name: recipe-mysql
  labels:
    app: recipe
    tier: svc-mysql
spec:
  ports:
    - port: 3306
      targetPort: 3306
  selector:
    tier: bdd
  type: ClusterIP