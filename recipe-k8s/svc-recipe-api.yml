apiVersion: v1
kind: Service
metadata:
  name: recipe-api
  labels:
    app: recipe
spec:
  ports:
    - port: 8443
      targetPort: 8443
      nodePort: 30443
  selector:
    app: recipe
    tier: backend
  type: NodePort
