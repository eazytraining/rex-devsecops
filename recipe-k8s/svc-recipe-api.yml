apiVersion: v1
kind: Service
metadata:
  name: recipe-api
  labels:
    app: recipe
spec:
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30088
  selector:
    app: recipe
    tier: backend
  type: NodePort
