# prepare.yml
- hosts: all
  gather_facts: false
  tasks:
    - name: Installer les dépendances Python pour les modules Ansible Docker
      raw: |
        dnf install -y python3-requests python3-pip
        pip3 install docker
        #become: true

