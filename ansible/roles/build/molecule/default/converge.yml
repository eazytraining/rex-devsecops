---
- name: Converge build-image
  hosts: localhost
  gather_facts: false
  vars:
    tag_api: v1
    tag_front: v1
    api_src_path: "{{ lookup('env','HOME') }}/rex-devsecops/recipe-api"
    frontend_src_path: "{{ lookup('env','HOME') }}/rex-devsecops/recipe-app"
  roles:
    - role: ../../../build

